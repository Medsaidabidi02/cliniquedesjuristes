# ========================================================================
# Backend Environment Configuration Template
# Copy this file to .env and update with your actual values
# ========================================================================

# ========================================================================
# Server Configuration
# ========================================================================
PORT=3000
NODE_ENV=production

# ========================================================================
# Database Configuration
# UPDATE THESE WITH YOUR ACTUAL CPANEL DATABASE CREDENTIALS
# ========================================================================
# Format: mysql://username:password@host:port/database
# Get these from cPanel > MySQL Databases
DATABASE_URL=mysql://c2668909c_clinique_user:YOUR_PASSWORD_HERE@localhost:3306/c2668909c_clinique_db

# Alternative: Individual parameters (if not using DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=c2668909c_clinique_user
# DB_PASSWORD=YOUR_PASSWORD_HERE
# DB_NAME=c2668909c_clinique_db

# ========================================================================
# Security Configuration
# IMPORTANT: Use strong, unique secrets for production
# ========================================================================
JWT_SECRET=legal-education-platform-super-secret-key-medsaidabidi02-2025-mysql5-version-production
VIDEO_SECRET=legal-education-platform-super-secret-key-medsaidabidi02-2025-mysql5-version-production
JWT_EXPIRES_IN=24h

# ========================================================================
# File Upload Configuration
# ========================================================================
MAX_FILE_SIZE=5120
# Use absolute path for cPanel deployment
UPLOAD_PATH=/home/c2668909c/public_html/backend/uploads

# ========================================================================
# URLs Configuration
# UPDATE WITH YOUR ACTUAL DOMAIN
# DO NOT include port numbers (Passenger handles this)
# Use HTTPS if you have SSL certificate
# ========================================================================
FRONTEND_URL=https://cliniquedesjuristes.com
API_URL=https://cliniquedesjuristes.com
BASE_URL=https://cliniquedesjuristes.com

# ========================================================================
# Admin Credentials
# Default admin account created on first run
# CHANGE PASSWORD after first login!
# ========================================================================
DEFAULT_ADMIN_EMAIL=admin@cliniquedesjuristes.com
DEFAULT_ADMIN_PASSWORD=admin123

# ========================================================================
# Hetzner Object Storage Configuration (Optional)
# For S3-compatible object storage
# ========================================================================
# Set to "true" (lowercase) to enable
ENABLE_HETZNER=true

# Hetzner S3 endpoint (without bucket name)
HETZNER_ENDPOINT=https://nbg1.your-objectstorage.com

# Your S3 bucket name
HETZNER_BUCKET=cliniquedesjuristes

# S3 Access credentials (get from Hetzner console)
# HETZNER_ACCESS_KEY_ID=your_access_key_here
# HETZNER_SECRET_ACCESS_KEY=your_secret_key_here

# ========================================================================
# HLS Video Streaming Configuration
# ========================================================================
# Set to "true" (lowercase) to enable HLS streaming
ENABLE_HLS=true

# ========================================================================
# NOTES FOR DEPLOYMENT
# ========================================================================
# 1. Replace 'c2668909c' with your actual cPanel username prefix
# 2. Replace 'YOUR_PASSWORD_HERE' with your actual database password
# 3. Replace 'cliniquedesjuristes.com' with your actual domain
# 4. Update UPLOAD_PATH with your actual cPanel home directory path
# 5. Change DEFAULT_ADMIN_PASSWORD after first login
# 6. For production, consider using environment-specific secrets
# 7. Never commit this file to version control (it's in .gitignore)
# ========================================================================

# ========================================================================
# PASSWORD SPECIAL CHARACTERS ENCODING
# ========================================================================
# If your database password contains special characters, URL-encode them:
# ! → %21
# @ → %40
# # → %23
# $ → %24
# % → %25
# ^ → %5E
# & → %26
# * → %2A
# ( → %28
# ) → %29
# + → %2B
# = → %3D
# { → %7B
# } → %7D
# [ → %5B
# ] → %5D
# | → %7C
# \ → %5C
# : → %3A
# ; → %3B
# " → %22
# ' → %27
# < → %3C
# > → %3E
# , → %2C
# ? → %3F
# / → %2F
# ========================================================================
