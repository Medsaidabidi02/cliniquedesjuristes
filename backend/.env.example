# ==========================================
# CRITICAL: Replace ALL placeholder values!
# ==========================================

# Server Configuration
PORT=5001
NODE_ENV=development

# Database Configuration
# REPLACE with your actual database credentials
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Security
# REPLACE with a strong random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# ==========================================
# HETZNER CONFIGURATION - REQUIRED FOR VIDEO PLAYBACK
# ==========================================
# Videos and thumbnails will NOT work without these settings!
# To find your values:
# 1. Log in to Hetzner Cloud Console
# 2. Go to Object Storage section
# 3. Copy your endpoint URL, bucket name, and credentials
# ==========================================

# MUST be exactly "true" (lowercase) to enable
ENABLE_HETZNER=true

# Your Hetzner endpoint (find in Hetzner console)
# Example: https://fsn1.your-objectstorage.com
# REPLACE "your-objectstorage.com" with your actual endpoint!
HETZNER_ENDPOINT=https://fsn1.your-objectstorage.com

# Your bucket name (create in Hetzner console)
# Example: my-video-content
# REPLACE "your-bucket-name" with your actual bucket!
HETZNER_BUCKET=your-bucket-name

# S3 Access Credentials (find in Hetzner console)
# These are optional if your bucket is fully public
# But recommended for better security and bucket management
HETZNER_ACCESS_KEY_ID=your-access-key-id
HETZNER_SECRET_ACCESS_KEY=your-secret-access-key

# ==========================================
# HLS Streaming - Must be enabled
# ==========================================
ENABLE_HLS=true

# URLs
FRONTEND_URL=http://localhost:3000
API_URL=http://localhost:5001
BASE_URL=http://localhost:5001

# Admin Credentials
DEFAULT_ADMIN_EMAIL=admin@test.com
DEFAULT_ADMIN_PASSWORD=admin123

# ==========================================
# VERIFICATION CHECKLIST:
# ==========================================
# [ ] ENABLE_HETZNER is set to "true" 
# [ ] HETZNER_ENDPOINT starts with "https://" and is YOUR endpoint
# [ ] HETZNER_BUCKET is YOUR bucket name (not "your-bucket-name")
# [ ] HETZNER_ACCESS_KEY_ID is YOUR access key (optional for public buckets)
# [ ] HETZNER_SECRET_ACCESS_KEY is YOUR secret key (optional for public buckets)
# [ ] DATABASE_URL has your actual database credentials
# [ ] You have run: npm install
# [ ] After saving, restart backend with: npm run dev
# [ ] Check logs for: "🎬 Hetzner HLS enabled: true"
# ==========================================
